title: 'Work skills'
sections:
  main:
    display: Main
    fields:
      -
        handle: title
        field:
          type: text
          required: true
          validate:
            - required
  sidebar:
    fields:
      -
        handle: slug
        field:
          type: slug
          required: true
